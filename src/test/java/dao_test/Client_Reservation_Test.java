package dao_test;

import java.util.Date;

import dao.IDao;
import dao.ReservationDaoImpl;
import models.Client;
import models.Reservation;

public class Client_Reservation_Test {

	public static void main(String[] args) {
		
		/*IDao<Reservation> resDao=new ReservationDaoImpl();
		Reservation r=new Reservation(new Date());
		r.setClient(new Client("Saad", "Zaynoune", "06345643", "mr", 9883716L));
		resDao.save(r);*/
		
	}
}
